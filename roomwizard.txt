mau@fury15:~/repos/roomwizard$ ./roomwizard.sh

════════════════════════════════════════
 RoomWizard
════════════════════════════════════════
  1) Commission an SD card        (offline)
  2) Set up a booted device       (ssh)
  3) Deploy apps                  (ssh)
  4) Device status                (read-only)
  5) Full bring-up: 1 -> 2 -> 3
  6) Commission COMPLETELY offline, one boot  (needs a bundle)
  q) Quit

Choice: 1   

════════════════════════════════════════
 1. Commission an SD card (offline)
════════════════════════════════════════
  This runs OFFLINE against the SD card, not the device. Before continuing:

    - the card must be out of the RoomWizard and in this host's reader
    - its rootfs (p6) must be mounted. The script finds it by content, so any
      mount point works; if it cannot, it names the disk it found and prints
      the mount command. You can also `export ROOTFS=/mnt/rw`.
    - you will be asked for sudo

  It sets the root password, the host name, SSH access and DHCP.

Card mounted and ready? [y/N]: y

================================================
  RoomWizard SD Card Commissioning Script
================================================

[INFO] Locating the RoomWizard rootfs among the mounted filesystems...
[SUCCESS] Found rootfs at: /run/media/mau/8acefb02-a8fb-4a2c-b5c7-a247ec76b504
[INFO] Firmware: SteelCase RW20 Embedded Platform (Yocto) 3.1.4 (build 20180309123456)

================================================
  Root Password Configuration
================================================

Enter desired root password: 
Confirm root password: 
[INFO] Generating password hash...
[SUCCESS] Password hash generated.

[INFO] Updating /etc/shadow with new root password...
[sudo: authenticate] Password:            
[INFO] Created backup: /run/media/mau/8acefb02-a8fb-4a2c-b5c7-a247ec76b504/etc/shadow.backup
[SUCCESS] Root password updated in /etc/shadow

================================================
  Host Name
================================================

[INFO] The card currently claims the name: null
[INFO] A unique name lets you reach the unit as <name>.local once mDNS is on
[INFO] (commissioning/provision.sh enables it), instead of hunting for a DHCP lease.

Host name [null]: rwtest
  host name: null -> rwtest
  /run/media/mau/8acefb02-a8fb-4a2c-b5c7-a247ec76b504/etc/hostname and /run/media/mau/8acefb02-a8fb-4a2c-b5c7-a247ec76b504/etc/hosts updated (backups: *.backup)
  and /run/media/mau/8acefb02-a8fb-4a2c-b5c7-a247ec76b504/etc/dhclient.conf updated — the DHCP server will now record 'rwtest'
[SUCCESS] Host name set to: rwtest

================================================
  SSH Configuration
================================================

[INFO] Configuring SSH to allow root login...
[INFO] Created backup: /run/media/mau/8acefb02-a8fb-4a2c-b5c7-a247ec76b504/etc/ssh/sshd_config.backup
[SUCCESS] SSH configuration updated.

[INFO] Verifying sshd is enabled at boot...
[SUCCESS] sshd is already configured to start at boot.

================================================
  SSH Key Setup (Optional)
================================================

Do you want to set up SSH key authentication? (y/n): y
[INFO] Found SSH public key: /home/mau/.ssh/id_rsa.pub
Use this key? (y/n): y
[INFO] Setting up SSH key authentication...
[SUCCESS] SSH key installed successfully.
[INFO] You will be able to SSH without a password using your key.

================================================
  Network Configuration (DHCP)
================================================

[INFO] Configuring eth0 to use DHCP...
[INFO] Created backup: /run/media/mau/8acefb02-a8fb-4a2c-b5c7-a247ec76b504/etc/network/interfaces.backup
[SUCCESS] eth0 configured for DHCP.

================================================
  Commissioning Complete!
================================================

[SUCCESS] All configuration changes have been applied.


  Remaining steps
  ────────────────
  1. Unmount SD card:       sync && sudo umount <mountpoint>

  2. Reinsert SD card into device, connect Ethernet, power on

  3. Wait ~30 s, find device IP from router DHCP leases
     (after step 5 the device also answers to NAME.local, where NAME is
      the host name you just set)

  4. If SSH key was NOT installed during commissioning: nothing to do here.
     Every script's SSH gate now tells "device is down" from "device is up
     and refused our key", and on the second it offers to generate a key
     and ssh-copy-id it for you. To do it by hand anyway:
       ssh-copy-id -i ~/.ssh/id_rsa.pub root@<ip>

  5. One-time system setup. Stops the vendor services, installs the app
     launcher, enables mDNS, DEEP-CLEANS the vendor software and raises the
     USB power budget on p1. Ends in a reboot:
       ./commissioning/provision.sh <ip>

     It asks once whether the card is backed up, before the first write.
     Neither the clean nor the p1 write is undoable on the device, and the
     472 MB factory-restore payload goes with the rest.
       --no-clean       delete nothing
       --no-usb-power   leave p1 alone (budget stays at the vendor's 100 mA)

  6. Deploy all apps:
       ./deploy-all.sh <ip>

  Or do steps 5-6 from the menu:   ./roomwizard.sh

  Full guide: COMMISSIONING.md


================================================

Press Enter to return to the menu... 

════════════════════════════════════════
 RoomWizard
════════════════════════════════════════
  1) Commission an SD card        (offline)
  2) Set up a booted device       (ssh)
  3) Deploy apps                  (ssh)
  4) Device status                (read-only)
  5) Full bring-up: 1 -> 2 -> 3
  6) Commission COMPLETELY offline, one boot  (needs a bundle)
  q) Quit

Choice: q

  ✓ Bye.
mau@fury15:~/repos/roomwizard$ 
