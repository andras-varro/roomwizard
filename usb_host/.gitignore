# Copyrighted device-specific files (extracted from device)
*.dtb
*.dts
kernel-config-*
devmem_write

# uImage files (large binaries from device boot partition)
uImage-system
uImage-system-patched
dtb_info.txt

# Kernel source and built modules (cross-compilation artifacts)
linux-4.14.52/
modules/
device_config
device_config.gz
*.ko
